import ROOT_DIRS         from './config/rootDirs';
import PATHS             from './config/paths';
import PORTS             from './config/ports';

module.exports = {
  rootDirs: ROOT_DIRS,
  paths: PATHS,
  ports: PORTS,
};
